# Point the client at your deployed server when testing.
# Copy to .env and set to your server URL (no trailing slash).
# Leave unset or empty to use the dev proxy (localhost:5000).
REACT_APP_API_URL=https://dbmanager.chames.dhibi.tn
